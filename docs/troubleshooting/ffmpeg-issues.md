# FFmpeg Issues

The CLI and the asset-prep script resolve `ffmpeg-static` from the workspace, so a global install is not required for the demos.

If extraction still fails:

- run `npx immersive-scroll doctor`
- confirm the source video downloads fully
- retry extraction after cleaning the output folder
