# Frame Mismatch

Mismatch symptoms:

- wrong frame count in manifest
- missing files in the public folder
- format mismatch between actual images and `manifest.json`

Use `npx immersive-scroll validate <frames-folder>` and regenerate the sequence when the fingerprint no longer matches the source clip.
