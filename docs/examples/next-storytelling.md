# Next Storytelling Example

The Next demo at `examples/next-demo` keeps the story route in App Router while the immersive runtime stays client-only.

Use it as the reference when you need:

- App Router compatibility
- public asset paths
- clean server/client separation
