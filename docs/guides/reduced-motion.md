# Reduced Motion

Do not treat immersive scroll as mandatory UX.

Recommended behavior:

- disable smoothing when reduced motion is requested
- reduce or skip frame changes entirely for accessibility-sensitive pages
- keep foreground content readable without the moving background
- avoid pinning if it turns the scene into a blocker on touch devices
