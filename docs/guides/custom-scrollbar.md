# Custom Scrollbar

The React, Next, and Solid demos all enable the built-in scrollbar to show how the progress layer can reinforce the cinematic feel.

Tune these fields:

- `scrollbar.enabled`
- `scrollbar.position`
- `scrollbar.width`
- `scrollbar.trackOpacity`
- `scrollbar.thumbOpacity`
- `scrollbar.autoHide`
