# Performance Tuning

Use these levers first:

- lower extraction resolution before lowering visual quality
- reduce fps for story clips that do not need film-like motion
- keep `preloadCount` conservative
- prefer one strong immersive section over many stacked ones
- validate frame folder size before committing demo assets to the repo
