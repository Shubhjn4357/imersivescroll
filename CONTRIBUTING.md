# Contributing

1. Install dependencies with `pnpm install`.
2. Run `pnpm lint`, `pnpm typecheck`, and `pnpm test` before opening a pull request.
3. Keep package boundaries intact: framework-neutral logic belongs in `core` or `shared`.
